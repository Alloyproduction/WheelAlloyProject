<?xml version="1.0" ?>
<odoo>
    <data  >
        <record id="purchase_remind_email" model="mail.template">

	<field name="name">Purchase Remind Email</field>

	<field name="model_id" ref="alloy_purchase_order_approval.model_purchase_order" />

	<field name="email_from">${object.sudo().create_uid.email_formatted | safe}</field>

	<field name="email_to">	${object.Approver_leader_id.login},${object.employee_name_id.work_email}</field>


	<field name="subject">Purchase Order Ref :${object.name}</field>

	<field name="body_html" type="html">

<div style="margin: 0px;padding: 0px;" >
    <p style="margin: 0px;padding: 0px;font-size: 13px;"  >
        Dear ${object.partner_id.name} % if object.partner_id.parent_id: (${object.partner_id.parent_id.name}) % endif

Here is in attachment a request for quotation ${object.name} % if object.partner_ref: with reference: ${object.partner_ref} % endif from ${object.company_id.name}.

If you have any questions, please do not hesitate to contact us.

Best regards,


    </p>


</div>

    </field>
            <field name="report_template" ref=""></field>
            <field name="report_name">Purchase${object.name_seq}</field>
</record>




    </data>



</odoo>